# offline_youtube_summarizer
Offline YouTube Video Summarizer that downloads audio from a YouTube URL, transcribes it and generates a concise summary with an offline model. Fully offline AI pipeline with CLI and optional Flask web interface.
